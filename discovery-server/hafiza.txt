# 🔍 ABSOLUTE BLUEPRINT: discovery-server (PORT 8761)

Bu belge, Spring Cloud Netflix Eureka sunucusunun src/main/java/com/mustafa altındaki sınıfını listeler. Tüm mikroservisleri barındıran rehberdir. (Sadece tek bir sınıfa hitap eder).

---

## 1. PAKET: `starter`
*   **`DiscoveryServerApplication.java`**: 
    *   `@SpringBootApplication`
    *   `@EnableEurekaServer`
    *   Ayağa kalktığında `bank-auth-micro`, `Bank-Demo-Backend` vs. gibi client'ların `/eureka/apps` adresine yaptıkları kayıtları hafızaya alır, loadbalancer işlemlerini organize eder. Kodun devamı tamamen `pom.xml` ve `application.yml` bağımlılığına yataral işlenir.
